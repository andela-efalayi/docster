import chai from 'chai';
// import Model from '../../models';

const expect = chai.expect;
// const User = Model.User;

describe('UserRoutes Demo test', () => {
  it('should run', () => {
    expect('UserRoutes Demo test').to.be.a('string');
  });
});
